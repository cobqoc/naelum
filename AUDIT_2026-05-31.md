# 낼름 전수 감사 — 2026-05-31 (밤샘 코드 전체 + 라이브)

> 사용자 지시: 비로그인부터 프로젝트 전체를 **이래도 되나 싶을 정도로 꼼꼼하게**. **문제만 찾고 수정 안 함.**
> 방법: 도메인별 병렬 심층 코드 감사 8기 + 프로덕션 빌드 라이브 테스트.
> 검증 기반: **e2e 443 passed (1 flaky→재시도 통과, 2 skip)** + lint 0 + build OK. 아래는 **그 외** 발견분.
>
> 심각도: 🔴 Critical(데이터유실·보안·크래시·법적) · 🟠 High(기능 오동작) · 🟡 Medium(엣지·UX) · 🔵 Low(사소·표기)
> "추정" = 코드 정독 기반 추론, 라이브 미확인. **모두 미수정 — 발견 기록만.**

---

## 0. 최우선 요약 (🔴/🟠 핵심)

| # | 심각도 | 영역 | 위치 | 한 줄 |
|---|---|---|---|---|
| C1 | ✅**수정됨** | 인증 | `proxy.ts:249` | ~~온보딩/약관 게이트가 `/api/*` 전체 제외 → 미동의 세션이 쓰기 API 직접 호출~~ → **2026-05-31 수정**: 게이트를 변경(POST/PUT/PATCH/DELETE) API에도 확장(`/api/auth/*`·GET 예외), 미동의 시 403 `onboarding_required`. 온보딩 완료=직접 supabase write라 안 막힘. 전용 e2e `onboarding-gate.spec.ts` + 풀 e2e 446 passed 회귀 0 |
| C2 | ✅**수정됨** | 에러 | `app/error.tsx` | ~~루트 에러바운더리가 `useI18n()` 호출 → Provider 밖이라 자기 throw → 흰 화면~~ → **2026-05-31 수정**: `useI18n()` 제거, URL 경로(/ko·/en…)에서 context-free 언어 감지 + 기존 정적 messages 맵 사용. **라이브 검증**: [lang]/layout throw 유발 → 루트 error.tsx가 ko/en 에러 UI 정상 렌더(흰 화면·useI18n throw 없음) |
| C3 | ✅**수정됨** | PWA | `public/sw.js:102` | ~~레시피 캐시 정규식이 locale prefix(`/ko/`) 누락 → 분기 영영 미실행~~ → **2026-06-01 수정**: 정규식에 locale prefix(`/(?:ko\|en\|…)?`) 허용 + `CACHE_VERSION` v14→v15. **라이브 검증**: `/ko/recipes/{id}` 방문 시 `naelum-recipes-v15` 캐시 생성·레시피 200/94KB 저장 확인(이전엔 캐시 자체가 안 생김). ⚠️ 최종 오프라인 동작은 Preview URL 권장(SW는 dev 재현 불가) |
| C4 | 🔴 | 배달 | `lib/delivery/api.ts:97` | 주문 가격(subtotal/total)을 클라 cart에서 그대로 INSERT, 서버 메뉴가격 대조 없음 → price tampering (prod 점등 전 필수) |
| C5 | ✅**수정됨** | DB | dev 스키마 | ~~소셜 테이블 UNIQUE 제약이 **dev에만 없음**~~ → **2026-06-01 수정** (`20260601_social_unique_constraints_dev_drift.sql` — dev에 6개 제약 추가, prod와 1:1 일치 검증. dev 중복행 0 확인. 멱등 마이그레이션이라 prod 재적용도 안전) |
| H1 | 🟠 | 레시피 | `app/api/recipes/[id]/route.ts:103` | PUT mass-assignment — `...recipeData` 통째 update → status·average_rating·cooked_count 등 카운터 스푸핑 |
| H2 | 🟠 | 업로드 | `app/api/recipes/[id]/complete/route.ts:18` | 완료사진 업로드가 **무검증(타입·크기·매직바이트 없음) public 버킷** + `lib/storage` 우회 + 매 요청 createBucket (3개 에이전트 공통 지적) |
| H3 | 🟠 | 소셜 | `user_blocks` 전반 | 차단 기능이 **아무 효과 없음** — blocked_id 필터가 댓글·프로필·추천 어디에도 없음 (안전기능 placebo) |
| H4 | 🟠 | 인증 | signin 경로 | 2FA(TOTP) 설정·검증 라우트는 완비됐으나 **로그인에서 전혀 강제 안 됨** → 켜도 비번만으로 로그인 |
| H5 | 🟠 | 매칭 | `recipes/[id]/page.tsx:96` | 보유재료 name↔id 평행배열을 **다른 필터로** 만들어 인덱스 zip 깨짐 → 상세페이지 대체재 chip에 **엉뚱한 보유재료 이름** (V2 정직성 위반) |
| H6 | ✅**수정됨** | i18n | `KitchenHomeClient.tsx` | ~~도감 허브 전체가 하드코딩 한글~~ → **2026-06-01 수정**: `ingredient.home` 네임스페이스 신설(8개 locale), 카테고리 라벨·countSuffix 재사용. `/en/kitchen` 한글 노출 제거 |
| H7 | 🟠 | 보안 | autocomplete 라우트 | 사용자 입력을 PostgREST `.or()` 필터에 비이스케이프 보간 → **필터 구문 주입**(`,`·괄호로 임의 컬럼 필터) |
| H8 | 🟠 | 보안 | autocomplete 라우트 | `status='approved'` 미필터 → pending/미승인 재료 자동완성 노출 (현 prod pending 0이라 잠복) |
| H9 | ✅**수정됨** | 법적 | `app/api/users/export/route.ts` | ~~GDPR export 30+테이블 전부 무페이지네이션 → 1000행 silent 절단~~ → **2026-06-01 수정**: `lib/supabase/fetchAll`(`.range()` 끝까지)로 37+7 테이블 전부 페이지네이션. sitemap·kitchen·추천·match-receipt도 동일 적용. vitest 5 신규 |
| H10 | 🟠 | 인증 | signin/signup 등 | rate limit이 스푸핑 가능한 `cf-connecting-ip`/`x-forwarded-for` 폴백 → CF 우회 origin 직결 시 IP 제한 무한 우회 (추정, 인프라 확인 필요) |
| ~~C6~~ | ✅반증 | PWA | `public/sw.js:7` | ~~STATIC_ASSETS offline.html 307 → addAll 거부 → SW install 실패~~ → **브라우저 라이브 검증으로 반증**: SW `naelum-static-v14` `activated`, offline.html 실제 캐시됨. addAll 거부 안 일어남 (§9-B) |
| H11 | 🟡 | 도감 | `ingredients/browse/route.ts:50` | **(라이브 발견)** "가공식품" 탭이 `is_processed=true` 가상 필터 → 치즈·생크림·마가린 등 본질 항목 혼입(도감 이중노출) + 모달 picker에서 이들이 본질 카테고리에서 제외되나 picker에 가공식품 칩 없음 (이번 세션 작업 충돌) |
| C7 | ✅**수정됨** | 장보기 | `shopping-list/route.ts:139` | ~~병합 시 비숫자 quantity("약간"등)가 NaN→null로 **기존 누적 수량 파괴**~~ → **2026-06-01 수정**: 파싱·병합을 `lib/shopping-list/quantity.ts` 순수함수로 분리(`parseQuantity`/`mergeQuantity` — 미상값은 기존 누적 절대 미파괴), vitest 9 신규. 같은 write 경로 UPDATE `.error` 미체크도 함께 보강 |
| C8 | ✅**수정됨** | 관리자 | `admin/inquiries/page.tsx:37` | ~~admin RLS 없음 → 운영자가 문의 못 봄+상태변경 원복~~ → **2026-06-01 수정**: prod·dev 실측 결과 admin 정책 진짜 부재 확인 → `20260601_contact_inquiries_admin_rls.sql`(admin SELECT/UPDATE, reports 패턴) **dev+prod 양쪽 적용·검증**. 페이지 `.error` 미체크도 옵티미스틱 롤백+토스트로 보강 |
| C9 | ✅**수정됨** | 보안 | `copyright/report/route.ts` | ~~rate limit·origin·인증 전무 → 이메일 폭탄·DB 스팸~~ → **2026-06-01 수정**: contact 라우트와 동일하게 `validateOrigin` + `checkRateLimit(copyright:ip, 5/시간)` 추가. 추가로 subject 헤더 인젝션(개행) `sanitizeHeader` 로 차단 |
| H12 | ✅**수정됨** | 알림 | `notifications/route.ts:104` | ~~DELETE RLS 정책 없음+`.error` 미체크 → 알림 부활~~ → **2026-06-01 수정**: DELETE 정책 추가(dev+prod), DELETE/PUT `.error` 체크 |
| H13 | ✅**수정됨** | DB | `ingredients_master` | ~~V2 단위계수 컬럼(grams_per_ml/count) dev 전용~~ → **2026-06-01 prod 승격**: 2컬럼 추가 + staple 계수 시드(식용유·참기름·우유·식초·달걀). prod V2 read 쿼리 에러 0 검증. prod엔 cooking_tools·relations·admin함수·recipe_ingredients 49% 링크 이미 존재 → 스키마 갭은 이 2컬럼뿐이었음. **merge 스키마 안전성 확보** |
| H14 | 🟠 | 전역 | `Common/Autocomplete.tsx:78` | **(2차)** 자동완성 stale-response race(in-flight 가드 없음) → 빠른 타이핑 시 이전 결과가 최신 위에 덮임 |
| H15 | ✅**수정됨** | 관리자 | `admin/users/[id]/route.ts:78` | ~~profiles DELETE 정책 없음+auth.users 미삭제 → 재로그인 가능~~ → **2026-06-01 수정**: service-role `auth.admin.deleteUser()`로 auth.users 삭제(profiles CASCADE), 본인 자기삭제 가드 |
| H16 | 🟠 | 팁 | `tip/[id]/route.ts:96` | **(2차)** draft 팁을 편집으로 발행 불가(is_draft 미전송) |

> **C2·C3·C6는 2026-05-11 i18n path 마이그레이션 때 따라가지 못한 회귀**로 보임 — 셋 다 CLAUDE.md에 명시적으로 경고된 함정의 실제 재발.
> **H7(필터 주입)은 라이브에서 실 익스플로잇 미실증 → 🟡 하향** (§9 참조).
> **반복 근본원인 3개 — ✅ 2026-06-01 일괄 수정**: ① `.error` 미체크 write(preferences·rating·save·complete·folders·notifications) 명시 체크 ② `.select()` 1000행 무방비(export·sitemap·kitchen·추천·match-receipt) → `lib/supabase/fetchAll` 페이지네이션 ③ user-context write RLS 정책 누락(notifications DELETE·contact_inquiries admin dev+prod 적용; profiles DELETE는 admin이 service-role auth.users 삭제로 대체). CLAUDE.md가 명시한 함정 — 신규 라우트마다 재발하던 패턴을 근본 차단.

---

## 1. 인증·온보딩·미들웨어·세션

- 🔴 **C1 — 온보딩/약관 게이트 `/api/*` 전체 제외** | `proxy.ts:249` `if (user && !bare.startsWith('/auth/') && !pathname.startsWith('/api/'))`. 약관·연령(GDPR Art.8) 미동의(`onboarding_completed=false`) 세션이 페이지 JS 없이 `fetch('/api/recipes',{method:'POST'})` 등 쓰기 API 직접 호출 가능. 각 라우트가 `requireAuth`만 보고 온보딩/동의 재확인 안 하면 법적 게이트 무력화.
- 🟠 **H4 — 2FA 미강제** | 2fa setup/verify/disable + `verifyTOTP` + backup codes 완비, 그러나 signin 라우트·페이지·미들웨어 어디서도 totp/mfa 미참조(grep 0). `signInWithPassword` 성공 = 즉시 세션. 사용자에게 거짓 안전감.
- 🟠 **H10 — rate limit IP 스푸핑 우회** | signin/signup/check-email/find-email/reset-password 공통 `cf-connecting-ip || x-forwarded-for || x-real-ip`. CF 우회해 Vercel origin 직결 시 헤더 임의 세팅으로 매 요청 IP 변조 → 로그인 5회/15분 등 우회, 크리덴셜 스터핑. (추정 — Vercel deployment protection/CF-only allowlist 여부에 따라 실노출 갈림.)
- 🟡 **좀비 세션 약관 루프** | `proxy.ts:249-264` profile 없으면 매 페이지 `/auth/terms-agreement` redirect → terms가 `createProfile` insert 시 username/email unique 충돌하면 게이트 탈출 불가. callback엔 `deleteZombieUser` 정리 있으나 직접 페이지 진입 경로엔 없음. (추정, 드문 엣지.)
- 🟡 **username 임시생성 2전략 공존** | `lib/auth/profile.ts` `generateTempUsername`(`user`+8난수) vs 트리거 `user_<uuid12>` — 포맷 불일치. `.error`는 체크하므로 silent 유실 아님. 트리거 도입 후 대부분 dead path.
- 🟡 **계정 열거(account enumeration)** | `check-email/route.ts:26` 무인증으로 가입 여부+`auth_provider`까지 반환(rate limit 10/분뿐). signin 페이지도 클라에서 직접 조회. reset-password는 일부러 항상 200으로 막는데 check-email은 정반대로 노출. (추정 — UX trade-off 가능.)
- 🟡 **OAuth `next` open-redirect 미검증** | `callback/route.ts:31,118` `${baseUrl}${next}` 검증 없이 append. signin의 `redirectAfterLogin`은 `//evil.com` 방어하는데 callback은 안 함. 대부분 same-origin path化되어 실효 낮으나 정책 불일치.
- 🔵 redirect 파라미터에 lang prefix 포함 → 로그인 후 언어 바뀔 수 있음 (UX 미세).
- ✅ **양호**: test signin 백도어(VERCEL+TEST_SIGNIN_ALLOW 이중 가드), rate limit RPC(ON CONFLICT 원자적), `x-naelum-user-id` spoofing 방어, find-email 마스킹, i18n redirect 루프 없음.

## 2. 레시피 CRUD·작성/수정 폼·RLS

- 🟠 **H1 — PUT mass-assignment** | `recipes/[id]/route.ts:90,103` `...recipeData` 통째 `recipes.update()`, 서버 allowlist 없음. RLS는 행만 제한(컬럼 X), `status` CHECK 제약 없음. 소유자가 `average_rating:5`·`cooked_count:9999`·`views_count`·`status`·`is_remix`·`published_at` 직접 세팅 → 카운터 RPC 우회, trending/rating sort 오염. (`author_id`만 WITH CHECK로 차단.)
- 🟠 **H2 — 완료사진 업로드 무검증 + storage 우회** | `recipes/[id]/complete/route.ts:18-60` `listBuckets`/`createBucket`/`.from().upload()` 직접 호출(lib/storage 우회), **타입·크기·매직바이트 검증 0**, fileExt raw, public 버킷 매 요청 생성. 유일하게 무방비한 업로드 경로. (`/api/upload`·`upload-video`는 검증 완비 — 대조.)
- 🟡 **POST 카운터 주입** | `recipes/route.ts:125` `...recipeData` 스프레드로 생성 시 views/rating/cooked 주입 가능(status/published_at은 post-spread라 강제됨).
- 🟡 **visibility status 미검증** | `recipes/[id]/visibility/route.ts:12` body `status` 검증 없이 write, CHECK 제약 없음 → 임의 문자열 저장.
- 🟡 **자식 테이블 비원자적 재작성** | PUT `route.ts:122-196` ingredients/steps/tags delete-후-insert가 트랜잭션 아님. steps insert 실패 시 old steps 이미 삭제+new 미반영 → 반쪽 상태. POST도 parent 생성 후 child 실패 시 orphan.
- 🟡 **서버 validation은 title/description만** | 나머지(재료/단계 비어있음·음수 servings·100 태그·NaN quantity)는 클라만. 직접 API 호출로 malformed 저장. `parseFloat(q)||null`로 `"0"`·파싱불가 모두 silent null.
- 🔵 댓글 `image_url` 외부 URL 무검증(hotlink/추적픽셀), resolveIngredientId 무제한 select(1000행 잠복).
- ✅ **양호**: PUT/DELETE/visibility 소유권 체크+403, upload/upload-video 검증 완비, 자식 RLS 정책 존재, 비공개 레시피 RLS 보호.

## 3. 검색·자동완성·추천·재료 매칭 V2

- 🟠 **H5 — 상세페이지 name↔id 인덱스 zip 깨짐** | `recipes/[id]/page.tsx:96-98` `initialUserIngredients`(전체 행) vs `initialUserIngredientIds`(id null 제외 필터) → 길이/순서 불일치. `RecipeBrowseView.tsx:166` `userIngredientNameById`가 인덱스 zip → id에 잘못된 이름. 보유재료 중 ingredient_id null 1개만 있어도 대체재 chip에 **엉뚱한 보유재료명** 표시. V2 "추측 0" 정면 위반.
- 🟠 **H7 — PostgREST 필터 주입** | `search/autocomplete:37`·`ingredients/autocomplete:44`·`ingredients/browse:39`·`search/route.ts:68,103` 사용자 입력을 `.or('name.ilike.%${term}%,...')`·`aliases.cs.{${q}}`에 비이스케이프 보간. `,`·괄호·중괄호로 OR절/그룹/배열 리터럴 깨고 임의 컬럼 필터 주입. `sanitizeQuery`는 `[%_\]`만 제거. SQLi는 아니나 데이터 노출 위험.
- 🟠 **H8 — autocomplete pending 노출** | `search/autocomplete:37`·`ingredients/autocomplete:52`에 `status` 필터 없음(browse는 있음). pending 재료 자동완성 노출. 현 prod pending 0이라 잠복.
- 🟡 **알레르겐 base 상속 1단계뿐** | `lib/recommendations/fetchRelations.ts:78` 변형→base 1회만 따라감, 조부모 allergens 누락 → 2단계 계층에서 false negative(안전 critical). 현 데이터 1단계뿐이라 잠복.
- 🟡 **추천 `.select()` 1000행 잠복** | `recommendations/route.ts:161,176,337` recipe_ingredients 후보·트렌딩 cooking_sessions 무제한 select+전수순회. 인기 재료 1개로 1000행 초과 쉬움 → 후보 silent 누락.
- 🟡 **재료검색 total/dedup 불일치** | `search/route.ts:114` count는 recipe_ingredients 행수, data는 레시피 dedup, range는 dedup 전 기준 → 페이지 경계 누락/중복, 페이지네이션 깨짐.
- 🟡 **trending/meal_time 알레르기 미필터** | `recommendations/route.ts:332` ingredients/personalized만 filterByAllergies, trending/meal_time은 미적용 → 알레르기 사용자에 차단대상 노출(두 탭 비활성이라 잠복).
- 🟡 **resolveIngredientId 무제한** | `lib/ingredients/resolveIngredientId.ts:23` approved 전체 select 무제한, 1000개 초과 시 해석 silent 실패.
- 🔵 ingredients/autocomplete·match-receipt rate limit 없음 / 단위 미등록 시 count 오분류 silent / substitute ratio 단위변환 무시(근사, "같은 단위 가정") / 다국어 "물"(eau·水) fundamental 누락.
- ✅ **양호**: 변형 단방향성(형제 누수 없음, 테스트 가드), substitute 전이 미적용, ratio NULL 안전, allergyFilterV2 정확일치 false-positive 0.

## 4. 냉장고·보유재료·장바구니·배달

- 🔴 **C4 — 배달 가격 조작** | `lib/delivery/api.ts:97-115` subtotal/total/price 전부 클라 cart에서 INSERT, 서버 `delivery_menu_items.price` 대조 없음. RLS WITH CHECK는 user_id만 검사 → `total:100` 조작 주문 정상 저장. mock 결제라 현재 무해, 실결제 연동 시 금전 손실.
- 🟡 **배달 RLS 컬럼 무제한 UPDATE** | `20260517_delivery_orders.sql:225` "cancel" 정책이 FOR UPDATE에 컬럼/상태 제약 없음 → 손님이 status·total·rider_id 임의 수정. 트리거는 전이 그래프만 막고 역할 구분 안 함. (prod 미적용 휴면.)
- 🟡 **라이더 self-assign 공백 + 점주 임의 배정** | `:237` rider_id NULL→본인 배정 정책 없음(셀프 디스패치 RLS 거부), 점주는 임의 user를 라이더로 강제 배정 가능. (prod 미적용.)
- 🟡 **inferStatus vs DB status 불일치** | `lib/delivery/api.ts:212` `cancelOrder`는 무조건 cancelled UPDATE인데 picked_up/delivered는 전이 그래프에 없어 throw. `inferStatus`가 경과시간만으로 화면상태 표시 → 화면("배달중")과 DB("paid") 불일치로 취소 가부 어긋남.
- 🟡 **user_ingredients quantity 서버 미검증** | `user-ingredients/route.ts:64`·`[id]:21` 음수·0·NaN·거대값·문자열 통과(클라 IngredientForm만 막음). API 직접/add-to-ingredients 무방비.
- 🟡 **shopping-list parseFloat NaN 합산** | `shopping-list/route.ts:139,143,151` `"약간"`·`"한 줌"` 등 비숫자 quantity → `parseFloat`=NaN, `currentQty+NaN=NaN` 저장 → 이후 수량 산수 전부 NaN 전파. 한국 레시피 다수.
- 🔵 ShoppingCartDropdown 옵티미스틱 PATCH 롤백/`.error` 미체크 → 화면-DB 불일치+캐시 오염 / 데모 냉장고 로그인 시 DB 마이그레이션 없음(체험 재료 유실 체감, 의도 가능) / cart cross-tab last-write-wins.
- ✅ **양호**: useSyncExternalStore 모듈캐시 reference 안정, pendingDeleteIdsRef race(debounce+cancelled+undo창) 방어, 냉장고 delete 옵티미스틱 `.error`+재발행 복원, cart share 토큰.

## 5. 소셜·프로필·설정·계정삭제

- 🔴 **C5 — dev↔prod 스키마 드리프트** | dev에 recipe_saves/likes/ratings/comment_likes/user_follows/user_blocks UNIQUE 제약 전부 없음(prod엔 있음, cooking_sessions만 양쪽 없음). dev에서 race 중복행 생성 가능 → 회귀 테스트가 dev green이어도 prod 다른 동작. CLAUDE.md "dev/prod 동기화 완료" 기재와 불일치. (추정 — dev 리셋 시 유실.)
- 🟠 **H3 — 차단(block) 무효과** | `user_blocks`는 쓰기/읽기만, blocked_id/blocker_id 필터가 댓글·답글·프로필·레시피·추천 어디에도 없음(grep). 차단해도 상대 댓글 보이고 내 글에 계속 댓글·평점·신고 가능. 안전기능 placebo.
- ✅**수정됨(2026-06-01)** **H9 — GDPR export 1000행 절단** | `lib/supabase/fetchAll`(`.range()` 끝까지 페이지네이션, `.error` throw)로 export 37+7 테이블 전부 적용 → 활동 많은 사용자도 완전 export. sitemap·kitchen/all·kitchen/summary·추천 후보·match-receipt 동일 적용. vitest 5 신규.
- 🟠 **팔로우 기능 전체 미구현** | user_follows 테이블·RLS·export는 있으나 follow/unfollow API 라우트·UI 전무. 명세 대비 죽은 스키마. (의도 가능.)
- 🟡 **toggle_recipe_like SELECT-then-INSERT race** | SECURITY DEFINER 아님+락 없음 → 동시 요청 둘 다 "없음"으로 읽어 INSERT 2회. dev 중복행, prod 23505 throw→500. likes_count drift.
- 🟡 **다수 write `.error` 미체크** | save(insert/delete), rating(update/insert), preferences(6개 delete+insert), complete(rating insert/update, cooking_sessions update). preferences는 delete 성공+insert 실패 시 **선호 전량 유실**인데 `{ok:true}`.
- 🟡 **존재하지 않는 RPC 호출** | comments POST `increment_comments_count`·DELETE `decrement_recipe_comments_count` dev·prod 모두 미정의(`.error` 미체크 silent 실패). 카운트는 트리거가 우연히 관리하나, **soft-delete(답글 있을 때)는 트리거 미발동 → comments_count 차감 안 됨**(삭제 댓글이 계속 집계).
- 🟡 **cooked DELETE 전체 세션 삭제** | `recipes/[id]/cooked/route.ts` DELETE가 매칭되는 모든 완료 세션 삭제(UNIQUE 없음). 한 레시피 여러 번 만든 기록을 "취소" 1회로 전부 유실.
- 🟡 **계정삭제 후 잔존 PII** | reports.description·contact_inquiries 본문·search_history가 SET NULL로 행 본문 보존. reports.reported_id는 FK 없는 다형성 → 탈퇴 시 dangling. GDPR 삭제권 관점 자유서술 PII 검토 필요.
- 🟡 **신고 중복방지 status='pending' 한정** | resolved/rejected 후 무제한 재신고(rate limit 5/시만 방어).
- 🔵 댓글 POST 길이 무제한(PUT은 1000자) / sanitizeHtml 정규식 한계(DOMPurify 권장, 현재 텍스트 렌더라 실XSS 낮음) / recipe_comments.image_url 죽은 필드.
- ✅ **양호**: 댓글·프로필 소유권 검증, 알림 INSERT service-role+`.error`, 자기 신고/차단 방지, 프로필 카운트 `count:exact,head:true`(1000행 회피), 비공개 레시피 RLS.

## 6. API 횡단·보안·RLS·rate-limit (인프라 전반 양호, 결함은 패턴 단위)

- 🟠 **`.error` 미체크 write 다발** (CLAUDE.md 규율 위반): `users/me/preferences`(6건, 최우선 — 선호 전량 유실) · `notifications`(PUT/DELETE) · `recipes/[id]/rating` · `complete` · `save`(count drift) · `shopping-list`(Promise.all) · `folders/[id]`. (best-effort 명시된 track/history/view/push는 수용 가능.)
- 🟡 **무제한 `.select()` 1000행 잠복**: `match-receipt:168`(재료 전체+무거운 매칭) · `kitchen/all:22`·`kitchen/summary:31`(items.length 카운트). 현 65개라 무사, 1000개 성장 시 silent 누락.
- 🟡 **조회수 조작** | `recipes/[id]/view` 비인증 POST rate limit 없음 → 무한 부풀리기(트렌딩 부활 시 조작 벡터).
- 🟡 **rate limit 미적용**: view·match-receipt(무인증+무거움)·copyright/report(무인증+Resend 이메일 → 이메일 폭탄)·save(형제 like는 30/분 — 비일관).
- 🟡 **입력 검증 부재**: match-receipt body text 길이 무제한(CPU) · copyright/report 무인증+무검증(contact_inquiries 스팸).
- 🔵 38개 라우트가 `error.message`/`details: uploadError` 클라 반환(제약명/컬럼명 노출) · `recipes/[id]/ratings`가 리뷰어 user_id(UUID) 공개(프라이버시 미세).
- ✅ **양호 (인프라 위생)**: in-memory 서버리스 상태 **없음** · service-role 9곳 전부 정당(cron CRON_SECRET, export 본인검증, share 토큰게이트) · 클라 번들 노출 없음 · `parsePagination` max 100 클램프 · CSP/HSTS/X-Frame 완비 · CORS same-origin · Promise.race+setTimeout 우회책 없음 · 빈 catch 거의 없음.

## 7. i18n(8 locale)·요리도감

- ✅ **8 locale key shape 완전 일치** (1645 keys flatten diff, 누락/잉여 0, 보간 변수 일치). mushroom·seaweed 신규분 21 categoryLabels 전 locale 정상.
- ✅**수정됨(2026-06-01)** **H6 — KitchenHomeClient i18n 0%** | `ingredient.home` 네임스페이스 신설(8개 locale 동시), 카테고리 라벨은 `ingredient.categoryLabels`·카운트는 `countSuffixLabel` 재사용(단일 출처). emoji·색상 톤만 CATEGORY_META 유지(심볼). `/en/kitchen` 한글 노출 제거(build typecheck로 8 locale shape 검증).
- 🟠 **카테고리 라벨 3곳 불일치** (CLAUDE.md "장·양념/소스" 트랩 재발): grain = 곡물류(i18n) / 곡류·면(hub) / 곡물·면(groupItems) · snack·bakery·dairy도 상이. 이모지도 hub(🦐🥛) vs browse(🐟🧀) 불일치 → 허브 클릭→browse 진입 시 라벨·이모지 바뀜. **단일 출처 없음**(하드코딩 4벌).
- 🟠 **cart.categoryLabels 6개 누락** | egg·legume·mushroom·oil·seaweed·sweetener 없음(8 locale 동일). `groupItems.ts` CATEGORY_LABELS에도 없음 → 해당 재료가 "기타"로 오분류 + `CartItemList:85` 폴백이 하드코딩 한글 "기타"(groupItems.ts:31). 이중 버그. **(이 세션 신규 카테고리 작업과 직접 연관 — 후속 보강 대상.)**
- 🟡 **INGREDIENT_CATEGORIES 하드코딩 한글 + egg/legume 누락** | `IngredientAutocompleteTypes.ts:63` name 전부 한글, egg/legume 자체 없음 → 필터/자동완성 칩 한국어 노출+분류 불가.
- 🔵 CATEGORY_COLORS 6개 카테고리 색상 누락 / KitchenHome `${count}개` toLocaleString 미적용 / BrowseClient 숫자포맷 내부 불일치(:356 콤마 vs :409 raw) / kitchen/summary 1000행 잠복 / duplicate-email redirect lang prefix 유실.
- ✅ **의도 확인(비이슈)**: CJK 카운터 접미사 빈 문자열(서구권 정답) · 브랜드명 "낼름" 한글 유지 · 재료 item.name 한국어 DB명(콘텐츠 작성언어 저장 정책) · 배달 생짜 라우팅(prod 미적용 격리).

## 8. PWA·서비스워커·a11y·에러바운더리·성능

- 🔴 **C2 — 루트 에러바운더리 자기 throw** | `app/error.tsx:37` `useI18n()` 호출, Provider는 `[lang]/layout` 안쪽 → 루트 에러 시 컨텍스트 없어 재-throw → global-error/흰화면 escalate, **진짜 에러 은폐**. 아이러니하게 line 15-24에 안전한 정적 messages 맵이 이미 있는데 굳이 useI18n 추가 호출. (`[lang]/error.tsx`는 Provider 안이라 OK — 모순.)
- 🔴 **C3 — SW 레시피 캐시 dead** | `public/sw.js:102` 정규식 `/^\/recipes\/[^/]+$/`이 `/ko/recipes/abc` 안 매치 → Network-First+fallback 분기 영영 미실행 → 오프라인 시 곧장 offline.html, RECIPE_CACHE 무용. i18n 마이그레이션 회귀.
- 🟠 **SW CACHE_RECIPE 핸들러 호출처 0** | `sw.js:179` postMessage 보내는 클라 코드 없음 → 오프라인 레시피 저장 미와이어링(C3와 합쳐 오프라인 기능 전체 미구현).
- ✅**수정됨(2026-06-01)** **Push 아이콘 404** | `sw.js` 알림 icon/badge → `icon-192.png`(실존), ShareButton 카카오공유 fallback `/icon-512.png` → `/icons/icon-512.png`. CACHE_VERSION v15→v16.
- 🟠 **signin 모달 a11y 전무** | `ResetPasswordModal:67`·`FindIdModal:39` role/aria-modal/ESC/focus-trap/X aria-label 전부 누락(다른 모달은 완비 — a11y 라운드 누락). OnboardingWizard도 role/aria-modal/focus-trap 누락.
- 🟠 **카카오 공유 fallback 이미지 404** | `ShareButton:158` `${baseUrl}/icon-512.png` (실제 `/icons/icon-512.png`) → 썸네일 없는 레시피 공유 미리보기 깨짐.
- 🟡 SW 이미지 Cache-First 동적 업로드 stale(대부분 cross-origin skip이라 영향 작음) / trimCache FIFO(LRU 아님) / global-error raw error.message 노출(dev 가드 없음) / AllRecipesClient 페이지네이션 append dedup 없음(중복 key) / raw `<img>`×3 / ShareButton 하드코딩 한글 toast + useOutsideClick 미사용.
- 🔵 manifest "any maskable" 결합 / loading.tsx 하드코딩 한글(claude-in-chrome splash 트랩) / getNowServerSnapshot new Date(0).
- ✅ **양호**: RecipeDetailClient `loading:()=>null`(#418 모범) · 지도류 ssr:false 정당 · useSyncExternalStore reference 안정(#185 없음) · 대다수 모달 useEscapeKey+useFocusTrap+role 완비 · ShareButton Kakao SDK 수동 inject(#404 교훈 적용) · SW production-only 등록.

---

## 9. 라이브 런타임 테스트 (프로덕션 빌드 `npm run start` + curl)

> claude-in-chrome은 연결 브라우저 선택에 사용자 확인이 필요(사용자 취침)해, **프로덕션 서버에 실제 HTTP 요청**으로 무인 라이브 검증. 코드 감사 발견을 실증/반증하고 신규 발견 추가.

### 라이브로 **확정**된 발견
- 🟠 **아이콘 404 실증** | `/icons/icon-192x192.png`·`/icons/icon-72x72.png`·`/icon-512.png` → HTTP는 200이지만 `content-type: text/html`(앱셸 HTML, 16KB) = **실제 PNG 아님**. 파일시스템엔 `icon-192.png`·`icon-512.png`만 존재. SW push 아이콘·ShareButton 카카오공유 fallback 전부 깨짐 확정.
- 🟠 **H6 /en/kitchen 한글 노출 실증** | `/en/kitchen` HTML에 "세상의 모든", "재료·도구·기법", "준비 중" 한국어 그대로 포함. KitchenHomeClient i18n 0% 확정.

### 라이브 **신규 발견**
- ✅**수정됨(2026-06-01)** **SW install 실패 가능 (offline.html 307 → addAll 거부)** | `proxy.ts` `I18N_EXEMPT_PREFIXES`에 `/offline.html` 추가 → 라이브 검증 200(이전 307). `cache.addAll`이 redirected 응답 거부할 위험 제거. (원문: `public/sw.js:7` `STATIC_ASSETS=['/manifest.json','/offline.html']`, install이 `cache.addAll(STATIC_ASSETS)`. `/offline.html` → 307 redirect `/ko/offline.html`(i18n 미들웨어).) `cache.addAll`은 redirected 응답을 거부(`Cache.put` redirected 플래그 throw — 잘 알려진 SW 함정) → **install waitUntil reject → SW 미설치/미활성 → 캐시·오프라인·푸시 레이어 전체 무력화**. C3(레시피 캐시 정규식 dead)와 합쳐 **post-i18n SW 이중 부패**. (확정: offline.html이 STATIC_ASSETS에 있고 307. 실패모드는 문서화된 동작 — 실제 SW install 검증은 브라우저 필요.)
- 🟡 **browse "가공식품(processed)" 탭 = is_processed 가상 필터 → 본질 카테고리 항목 혼입** | `app/api/ingredients/browse/route.ts:50` `categories=processed`면 `.eq('is_processed', true)`(category='processed'가 아님). 라이브: `categories=processed` → **12개** = 진짜 가공식품 4(베이컨·소시지·스팸·햄) + **is_processed 플래그된 본질 항목 8**(치즈·생크림·마가린·다진고기·모짜렐라·체다·슬라이스치즈·다진돼지고기). **이번 세션 작업과 직접 충돌**: ① 도감에서 치즈·생크림 등이 dairy 탭 + 가공식품 탭에 **이중 노출** ② 모달 picker(`includePending=false`, `:56` `is_processed=false`)에선 치즈·생크림·마가린이 dairy/oil 탭에서 **제외**되는데 `INGREDIENT_CATEGORIES`에 'processed' 칩이 **없어**(picker 카테고리 목록 미포함) → 검색 타이핑으로만 찾을 수 있음. 이번에 2순위 재료에 `is_processed=true`를 단 결과가 picker UX에 영향. **결정 필요**: picker에 가공식품 칩 추가 / 단일원료 가공(치즈·생크림)의 is_processed 재검토 / browse의 processed=가상필터 의미 정리.

### 라이브로 **하향/반증**된 발견
- 🟡↓(원래 H7) **PostgREST 필터 주입 — 실 익스플로잇 미실증** | `?q=a,status.eq.pending`·`?q=a)` 라이브 → 200·`{"suggestions":[]}`·에러/누수 없음. 비이스케이프 보간은 잠재 리스크로 남으나 현 설정에서 크래시·데이터 노출 미발생. **High→Medium 하향.**
- ℹ️ **거짓경보 정정** | `?category=` 단수 param은 무시되어 전체 반환 — 올바른 param은 `categories=`(복수). 필터는 정상 동작(`categories=mushroom`→5 정확). 버그 아님.

### 라이브로 **양호** 확인
- ✅ 비로그인 주요 페이지 전부 200 (`/ko`·`/en`·kitchen·recipes·search·signin), `/`→`/ko` 307, `/ko/ingredients`→`/ko/kitchen` 308(도감 이전 정상)
- ✅ 보안 헤더 견고: `Content-Security-Policy`(상세 allowlist)·`Strict-Transport-Security`·`X-Frame-Options: DENY`·`X-Content-Type-Options: nosniff`
- ✅ robots.txt AI 크롤러(GPTBot 등) Disallow
- ✅ 핵심 API liveness: search·recipes·autocomplete 전부 200
- ✅ **이번 세션 작업 실증**: kitchen/summary 카테고리별 카운트 정상, `categories=mushroom`(5)·`seaweed`(3) 정확 필터

### 9-B. 브라우저 라이브 검증 (claude-in-chrome, 사용자 일반 Chrome — 2026-05-31 추가)
> 사용자가 깨어나 Chrome 연결 → 코드로 못 보는 4종을 실제 브라우저에서 검증. 프로덕션 빌드 `localhost:3000`. (claude-in-chrome 배경탭 splash hang은 `$RV($RB)` 강제 swap으로 우회 — 환경 제약이지 버그 아님.)

- ✅ **SW install — 정상 (C6 반증)** | `navigator.serviceWorker.getRegistrations()` → `naelum-static-v14` `state:activated`, waiting/installing 없음. `caches`에 `naelum-static-v14`·`naelum-images-v14` 존재하고 **`/offline.html`이 실제 캐시에 들어있음**. 즉 `cache.addAll(['/manifest.json','/offline.html'])`가 307에도 거부되지 않고 성공 → **C6(install 실패 가설) 반증.** (코드 기반 "documented footgun"이 이 Chrome에선 실제로 안 터짐.)
- ⚠️ **C3 부분 지지** | 캐시 키에 `naelum-recipes-v14` **없음** (레시피 네트워크-우선 캐시 브랜치 미작동). 단 레시피 페이지가 claude-in-chrome splash로 정상 fetch 흐름을 다 못 타서 결정적이진 않음 — 정규식 `/^\/recipes\/[^/]+$/`가 `/ko/recipes/…` 미매치라는 코드 사실은 유효. **오프라인 레시피 캐시 비작동 가능성 유지(중).**
- ✅ **콘솔 에러 — 없음** | 홈(`/ko`)·레시피 상세·검색(`/ko/search`)·`/en/kitchen` 전부 **앱 콘솔 에러 0**. 유일한 로그는 사용자 Chrome 확장(Grabbit/linkify) — 앱 무관.
- ✅ **#418 하이드레이션 — 관측 안 됨** | 위 페이지들에서 Minified React #418/#185·hydration mismatch 경고 없음. (dynamic ssr:false 6곳 전환·loading:()=>null 적용이 실효 있는 것으로 보임.)
- ✅ **IME(한글 조합) — 정상** | 검색창에 CompositionEvent 시퀀스(ㄱ→기→김→김ㅊ→김치) dispatch → `input.value="김치"` 정확, **조각("ㄱ"·"김ㅊ") 노출 0**, 검색 결과도 김치 관련 정상(가지김치·배추김치·총각김치·김치 — 이번 세션 추가 재료 포함). 2026-05-23 fragmentation 버그 재발 없음.
- ✅ **H6 /en/kitchen 한글 노출 — 실제 렌더 확정** | 영어 페이지(title "Kitchen Guide")인데 body에 "세상의 모든 재료·도구·기법을 한곳에서...", "육류"·"채소"·"발효식품"·"113개" 한글 그대로. 비한국어 사용자 직접 노출 시각 확정.

**요약**: 브라우저 4종 중 **SW install·콘솔·#418·IME 모두 양호**(C6 반증, 신규 버그 없음). 코드 감사의 High/Critical(C1 게이트·C2 error.tsx·C7 NaN·C8 문의RLS·C9 저작권·H1 mass-assignment 등)은 브라우저로 보이는 게 아니라 **여전히 유효** — 인증/특정 데이터 상태 필요.

### 9-C. 핵심 루프(냉장고→추천→레시피) 라이브 완주 — ✅ 정상
비로그인 데모 모드로 실제 클릭 완주:
1. ✅ 빈 냉장고 → "냉장고 채우기" 모달 → 검색("쌀", IME 조합)으로 쌀 staged → **"1개 추가" 커밋** → 냉장고에 "🍚 쌀" 반영
2. ✅ 홈에 **"🔥 바로 만들 수 있는 레시피 1개"** 추천 즉시 표시
3. ✅ 추천 클릭 → `/recipes?tab=ingredient` → **"단방향 substitute 테스트 — 쌀 필요 (죽)"이 "바로 가능" 매칭** (쌀 보유 → 죽 레시피 makeable 정확 판정)
4. ✅ 새로고침 후에도 쌀 + 추천 유지(영속성 OK), 루프 전체 **콘솔 에러 0**
- ⚠️ **초기 "관찰" 4종 → 전부 오진(테스트 환경 착오)으로 정정 (2026-05-31)**: 깨끗한 재확인(localStorage·캐시·SW 전수 리셋 + `location.reload()` 하드 리로드) 결과, **테스트 Chrome이 naelum에 로그인된 상태**(`sb-…-auth-token` 쿠키)였음이 확인됨. 즉 위 루프는 **로그인 사용자 경험**이었고, "비로그인 데모 모드"가 아니었다. 따라서:
  - ① 모달 staged→"N개 추가" 커밋 = 정상 배치 UX (버그 아님, 유지 권장).
  - ② "v4=[] 인데 유지" = **버그 아님**. 로그인 상태라 쌀이 **DB(`user_ingredients`)에 정상 insert**되어 영속(하드 리로드·localStorage 비움에도 유지). demo localStorage(v4)는 로그인 시 미사용이라 빈 게 정상.
  - ③ "DEMO(닭고기·버터) 안 보임" = **정상**. DEMO 기본값은 *비로그인 전용*. 로그인 사용자는 자기 DB 냉장고를 봄(추가 전엔 빈 상태=정상).
  - ④ "language=ja" = 정정대로 **코스메틱 무영향**(경로가 표시 결정, localStorage는 마지막 명시 선택 fallback). 리셋 후 null.
  - **결론: 코드 변경 불요.** 단 ⚠️ **로그인 사용자의 핵심 루프(냉장고 DB 추가→추천→매칭→레시피)는 라이브로 정상 작동 확인됨** — 이건 견고한 긍정 결과. **비로그인 데모 모드 루프는 미검증**(로그아웃 필요, 사용자 세션 영향 있어 보류).
  - 테스트 잔여물: 이 과정에서 로그인 계정 dev 냉장고에 **"쌀" 1건 추가됨**(제거 가능).

> **인증 흐름 라이브 검증 시도→차단**: `/api/test/signin`으로 H1(mass-assignment)·H3(차단)을 실제 재현하려 했으나 테스트 유저(`playwright-w*@naelum.local`)가 e2e 종료 후 정리되어 로그인 불가(Invalid credentials). 두 발견은 **코드 정독 기반 확정 유지**(라우트 `...recipeData` 스프레드·blocked_id 필터 부재 grep 0). 사용자 계정 또는 영속 테스트 유저로 재개 시 라이브 확정 가능.
> **라이브 미수행(브라우저 필요)**: 실제 클릭 흐름·콘솔 에러·렌더 깨짐·SW 실제 install 결과·#418 hydration·IME 조합. 사용자 일반 Chrome에서 claude-in-chrome 재개 시 보강 권장.

> **⚠️ 선존 e2e 실패 발견 (2026-06-01, 이번 세션 작업과 무관)**: `e2e/auth.spec.ts` 4개 — 로그인 페이지 UI 요소 확인·빈값 유효성·잘못된 이메일 거부·비밀번호 보이기/숨기기 토글 — 이 **single-worker·retries=0** 으로도 실패. **baseline 커밋 `024265a`(이번 세션 작업 시작 전)에서 동일하게 4 실패/3 통과 재현** → 이번 6개 수정과 무관한 **pre-existing 실패** 확정. `/signin` 폼 셀렉터(`form input[type=email/password]`·`button[type=submit]`)를 못 잡음 — signin 페이지 구조 변경 vs 옛 테스트 셀렉터(cart 스테일 패턴과 유사) 추정. 별도 백로그. 전체 448 run의 무더기 1.0m 타임아웃 = pre-existing 실패 + 2-worker dev DB contention.

---

---

# 2차 웨이브 (1차 8개가 깊이 안 판 영역)

## 10. 팁·폴더·식단·장보기

- 🔴 **장보기 병합 시 NaN이 기존 수량을 null로 덮어씀 (데이터유실)** | `shopping-list/route.ts:139,143` + `lib/hooks/useCartFromRecipe.ts:88`. recipe 재료 quantity가 `"약간"`·`"적당량"`·`"2~3"` 등 비숫자(한국 레시피 흔함) → `parseFloat`=NaN → `currentQty + NaN = NaN` → JSON 직렬화 시 `null`. **이미 "양파 3개"가 있는데 quantity="약간" 양파를 또 담으면 3→null로 파괴.** (§4 Medium의 격상 — 병합 분기는 기존 누적 파괴.)
- 🟠 **팁 draft→발행 편집으로 불가** | `tip/[id]/route.ts:96` PUT이 `is_draft` 안 건드림 + edit UI 미전송 → draft 팁은 편집·저장해도 영영 draft(공개 목록 안 뜸). 발행 경로 부재.
- 🟠 **폴더 저장 시 folder_id 소유권 미검증** | `recipes/[id]/save/route.ts:24,50` 클라 folder_id 그대로 insert, user 소유 확인 없음(RLS는 user_id만). 타인 폴더로 분류 가능(읽기는 막혀 영향 제한).
- 🟡 save write `.error` 미체크(토글-DB 불일치) / `recipe_folders.recipes_count` 미유지(항상 stale, 목록↔상세 개수 불일치) / 폴더명·다중 is_default 중복 제약 부재 / 비공개 팁 신고 불가(RLS로 404, 현 prod 팁 10건 전부 비공개라 신고 사실상 불능) / duration·quantity·color 입력검증 POST/PUT 비대칭.
- 🔵 `shopping_list_items` note/notes 중복 컬럼 / tip GET getUser 2회 / **`20260405_*.sql` 마이그레이션이 실 DB 스키마와 불일치**(신규 환경 재구축 불가, 알려진 divergence).
- ✅ **양호**: shopping/meal_plan/folder/tip write RLS 정책 존재, FK 삭제 규칙 적절(meal_plan_items·shopping_list_items·saves SET NULL), 팁 발행상태 CHECK 제약, 1000행 방어(.limit/count), 타인 팁 수정/삭제 차단.

## 11. 알림·푸시·Cron·Analytics

- 🟠 **알림 DELETE 조용히 실패 (사용자 체감)** | `notifications/route.ts:104` `notifications`에 **DELETE RLS 정책 없음**(SELECT·UPDATE만) → user-context delete가 0행 삭제+`.error` 미체크로 `{success:true}` → **삭제 눌러도 새로고침하면 알림 부활**. PUT(읽음)도 `.error` 미체크(UPDATE 정책은 있어 동작은 함).
- 🟠 **check-expiry self-insert 깨짐** | `ingredients/check-expiry/route.ts:72` notifications INSERT를 user-context로(INSERT RLS 정책 없음 → 거부). `lib/notifications/create.ts`는 service-role로 우회했는데 이 라우트만 안 따름. **현재 호출처 0 (dead route)** — 부활 시 즉시 깨짐.
- 🟡 events에 full user-agent 저장(지문/PII 소지, 단 consent 게이트 통과) / `recommendations/track`은 analytics 동의 무관하게 호출(기능 데이터 vs analytics 모호) / analytics payload에 사용자 입력 재료명 포함.
- 🔵 push subscribe DELETE·send-expiry select `.error` 미체크 / send-expiry opted-in profiles 1000행 제한(cron이라 silent) / SW push 페이로드·url same-origin 미검증(open-redirect 유사, VAPID 게이트라 난이도 높음).
- ✅ **양호**: cron 2개 CRON_SECRET+service-role, 알림 교차유저 insert는 create.ts가 service-role+`.error`, in-memory 상태 없음, events user_id 사칭 방지, VAPID private 노출 없음, admin analytics verifyAdmin+limit, track consent 게이트+sendBeacon fallback.

## 12. 관리자·신고·문의·저작권·SEO

- ✅**수정됨(2026-06-01)** **문의 관리 페이지 admin RLS 부재** | prod·dev `pg_policies` 실측 → 양쪽 모두 admin 정책 진짜 부재 확인(INSERT+본인SELECT만, MCP 수동 정책 없었음). `20260601_contact_inquiries_admin_rls.sql` 로 admin SELECT/UPDATE 정책 추가(reports 패턴 동일) → **dev+prod 양쪽 적용·4정책 검증**. 페이지 옵티미스틱 UPDATE 도 `.error` 체크→롤백+토스트로 보강(조용한 원복 제거).
- ✅**수정됨(2026-06-01)** **저작권 신고 API 무방비** | `copyright/report/route.ts` 에 `validateOrigin` + `checkRateLimit(copyright:ip, 5회/시간)` 추가(contact 라우트와 동일 패턴) → 이메일 폭탄·쿼터 소진·DB 스팸 차단.
- ✅**수정됨(2026-06-01)** **이메일 subject 헤더 인젝션** | `copyright/report` subject 에 들어가는 `reporter_name`/`reporter_email` 를 `sanitizeHeader`(개행·제어문자 제거·100자 제한)로 정제 → `이름\nBcc:...` 헤더 조작 차단.
- 🟠 **sitemap 1000행 제한** | `sitemap.ts:10` recipes `.select` 무제한 → published 1000개 초과 시 silent 누락(SEO 손실). profiles도 `.limit(1000)`만.
- 🟠 **admin 사용자 삭제 불완전** | `admin/users/[id]/route.ts:78` `profiles DELETE` 정책 없음 → 0행 삭제+success, `auth.users` 미삭제(service-role deleteUser 미호출) → **삭제해도 재로그인 가능**. ⚠️ prod 정책 검증 필요.
- 🟠 **robots 정적/동적 이중 출처** | `public/robots.txt`(정적, 우선 서빙) vs `app/robots.ts`(dead). 내용 다름 → 동적 수정 반영 안 됨. `Disallow: /admin/`이 실 경로 `/ko/admin/`·`/en/admin/` 미매칭(admin은 로그인 게이트라 노출은 안 됨).
- 🟡 레시피 metadata canonical/hreflang 부재(8언어 중복 콘텐츠 SEO 페널티 위험)·OG url bare(`/recipes/id`→307) / admin reports status allowlist 없음 / admin substitute-suggestions DELETE `.error` 미체크.
- ✅ **양호**: admin API 12라우트 전부 verifyAdmin/checkAdminRole(인가 갭 없음, 두 패턴 혼재는 부채), admin layout server 게이트, 사용자 신고 3종 requireAuth+rate limit+`.error`, reports/admin_actions/banned RLS 완비, Resend 키 서버전용, metadataBase 설정.

## 13. 대형 클라이언트 컴포넌트 (상태·effect·race)

- 🟠 **Autocomplete stale-response race (앱 전역)** | `components/Common/Autocomplete.tsx:78` 디바운스 effect에 in-flight 가드 없음(`clearTimeout`만) → 빠른 타이핑 시 **이전 검색어 결과가 최신 입력 위에 덮임**. IngredientAutocompleteV2(레시피 작성 재료입력)가 위임하는 공용이라 영향 넓음.
- 🟠 **useIngredientPicker 검색 race + updater 내 부수효과** | `lib/hooks/useIngredientPicker.ts:99` 디바운스·stale 가드 없는 fetch + `:112` `setPage` updater **안에서 fetch 실행**(Strict Mode 2회 → 중복 fetch).
- 🟡 AllRecipesClient fetch race(정렬·loadMore 동시 in-flight, append dedup 없음 → 중복 카드/정렬섞임) / IngredientPickerInline 편집 리스트 **index key**(중간 삭제 시 입력값·포커스 밀림) / IngredientPickerInline 레거시 outside-click(useOutsideClick 미사용).
- 🔵 useFridgeItems/Interactions 핸들러 unmount 후 setState(누수, cancelled 가드 없음) / RecipeBrowseView memoText prop 비동기화(실사용 무해) / 하드코딩 한글(Autocomplete "검색 결과가 없습니다" 등·IngredientAutocompleteV2 placeholder·**useVoiceGuide ko-KR TTS 강제**=다국어 사용자에 한국어 음성).
- ✅ **양호**: god-file race 분해 규율 위반 없음(pendingDeleteIdsRef 부모 소유·옵티미스틱 한 hook에 가둠), createClient 싱글톤, useMultiTimer interval 중복 가드·unmount 정리 정상.

## 14. DB 레이어 — dev↔prod 드리프트 (실측 비교)

> **핵심: dev↔prod가 양방향으로 어긋남** — 단일 마이그레이션 방향(dev→prod) 미준수 신호. (RLS 정책 매트릭스 전수는 미완 — 후속 권장.)

- 🟠 **`ingredients_master` 단위변환 계수 컬럼 dev 전용** | `grams_per_count`(jsonb)·`grams_per_ml`(numeric)이 **dev에만, prod 없음**. V2 양·단위 비교 코드를 main(prod)에 배포하면 `column does not exist`로 깨짐. (V2 prod 승격 = 이 컬럼 마이그레이션 선행.)
- 🟠 **`recipes` prod 전용 컬럼 + default 충돌** | prod에만 `carbs/fat/protein/sodium`(float8, 기존 `*_grams`와 **중복**·`sodium` vs `sodium_mg` 단위 1000배 상이 위험) + `review_status`. **`show_source` default dev=false / prod=true** → 신규 레시피 출처표시 기본값 환경별 상이(e2e 출처블록 검증 환경따라 갈림).
- 🔴(재확인) **소셜 테이블 UNIQUE 제약 dev 누락**(§5 C5) — recipe_saves/likes/ratings/comment_likes/user_follows/user_blocks.
- ✅ profiles 77컬럼 완전 일치.

### 14-A. RLS write 정책 갭 — 전수 매트릭스 (dev 실측, `pg_policies` × 62테이블)
> user-context로 write하는데 해당 cmd 정책이 없으면 **조용한 거부(데이터유실)**. 아래는 갭 + 앱이 실제 그 cmd를 쓰는지 교차.

| 테이블 | SELECT | INSERT | UPDATE | DELETE | 갭 영향 |
|---|:-:|:-:|:-:|:-:|---|
| **notifications** | ✅ | ❌ | ✅ | ❌ | 🟠 INSERT 없음(check-expiry self-insert 거부)·DELETE 없음(알림 삭제 조용히 실패=H12). **실측 확정** |
| **contact_inquiries** | ✅* | ✅ | ✅ | — | ✅ **2026-06-01 admin SELECT/UPDATE 추가(C8 수정, dev+prod)**. SELECT=본인+admin, UPDATE=admin |
| **profiles** | ✅ | ✅ | ✅ | ❌ | 🟠 DELETE 없음(admin 사용자삭제 조용히 실패=H15) |
| **reports** | ✅ | ✅ | ❌ | ❌ | 🟠 admin UPDATE/DELETE 없음 → admin reports PATCH가 user-context면 실패(service-role 사용 여부 확인 필요) |
| **recipe_notes** | ✅ | ❌ | ❌ | ❌ | 🟡 SELECT만 — 앱이 write하면 거부(레거시/미사용 추정, 확인 필요) |

- ✅ **정상(append-only·toggle이라 UPDATE/DELETE 불요)**: recipe_views·events(INSERT만), recipe_likes·comment_likes·user_blocks·user_follows(UPDATE 불요), reports INSERT, stores/ingredient_price_reports.
- ✅ **의도적 deny-all(정책 0 = service-role 전용)**: rate_limits·recipe_queue·search_queries·system_settings·channels·collection_logs.
- ⚠️ **prod 별도 확인**: contact_inquiries·profiles·reports의 admin 정책이 prod엔 MCP 수동 추가됐을 수 있음(dev/prod 드리프트 패턴). prod `pg_policies` 실측 권장.

---

## 부록 — 교차 확인된 다중 지적 (신뢰도 높음)
- `complete/route.ts` storage 우회+무검증 업로드: **레시피·소셜·API 3개 에이전트 공통**
- GDPR export 1000행 절단: **레시피·소셜 2개 공통**
- `.error` 미체크 write(특히 preferences): **소셜·API 2개 공통**
- cart.categoryLabels 6개 누락: **i18n 에이전트** (이 세션 카테고리 작업과 직접 연관)
